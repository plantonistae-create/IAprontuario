# NEXA v18.7.1 regression

- auditor workspace opens from every audit entry point
- legacy audit modal remains hidden except during native queue bridge harvesting
- professional dashboard remains the visible auditor UI
- mobile header is sticky and in document flow
- mobile clinical content no longer receives duplicated header top offset
- service worker and loader are versioned at v18.7.1
