# NEXA v18.7.1

- Force all Auditoria entry points to open the professional v18.7 workspace instead of the legacy audit modal.
- Keep the native `coreAuditBody` and RPC review actions as a hidden bridge/data source only.
- Convert the mobile NEXA header from fixed overlay to sticky document-flow header and move it before `#mainApp`.
- Remove duplicate mobile top offset so content cannot scroll underneath the header.
- Bump loader and service-worker cache to v18.7.1.
